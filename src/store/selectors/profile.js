export const profileSelector = (state) => state.profileSelector

export const profileNameSelector = (state) => state.profile.name

export const profileUser = (state) => state.profile

export const profileIsAuthed = (state) => state.profile.isAuthed